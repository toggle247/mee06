{
  "name": "mee06",
  "private": true,
  "version": "0.0.0",
  "packageManager": "bun@1.2.9",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "set:stack": "heroku stack:set container"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "turbo": "^2.5.3"
  },
  "peerDependencies": {
    "typescript": "^5.8.3"
  },
  "workspaces": [
    "servers/*",
    "web/*"
  ],
  "dependencies": {
    "@tsconfig/recommended": "^1.0.8"
  }
}
